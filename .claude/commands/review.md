# Review uncommitted and unpushed changes

Run `git status` and `git diff` to show all uncommitted changes, then run `git log origin/main..HEAD` to show unpushed commits.

Provide a thorough code review following the review standards in `.claude/agents/reviewer.md`.

Focus on:
- Code correctness
- Following project conventions
- Performance implications
- Security considerations
- Any potential issues or risks
