# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Rust fmt backup files
**/*.rs.bk

# MSVC Windows builds of rustc generate these files (debugging info)
*.pdb

# Default database directory used by the CLI (`--dir ./data`)
/data/
*.merge/

# Note: Cargo.lock is intentionally TRACKED — this is a binary crate, and
# binaries commit their lockfile for reproducible builds (Rust convention).
