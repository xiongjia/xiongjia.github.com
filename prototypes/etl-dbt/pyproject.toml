[project]
name = "etl-dbt"
version = "0.1.0"
description = "Local dbt-core + DuckDB ETL demo: mock raw data -> staging -> marts (with incremental)"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.10",
    "dbt-duckdb>=1.10",
    "duckdb>=1.2",
]

[tool.uv]
# Self-contained prototype env; excluded from the repo toolchain fmt/lint